<form method="post" action="{{ path('client_delete', {'id': client.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet patient?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id) }}">
	<button class="btn">Supprimer</button>
</form>
